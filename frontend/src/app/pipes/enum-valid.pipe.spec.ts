import { EnumValidPipe } from './enum-valid.pipe';

describe('EnumValidPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumValidPipe();
    expect(pipe).toBeTruthy();
  });
});
