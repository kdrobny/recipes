import { RecipeFilterDifficultyPipe } from './recipe-filter-difficulty.pipe';

describe('RecipeFilterDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeFilterDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
