import { RecipeFilterCategoryPipe } from './recipe-filter-category.pipe';

describe('RecipeFilterCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeFilterCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
