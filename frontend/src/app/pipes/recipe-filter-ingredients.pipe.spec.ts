import { RecipeFilterIngredientsPipe } from './recipe-filter-ingredients.pipe';

describe('RecipeFilterIngredientsPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeFilterIngredientsPipe();
    expect(pipe).toBeTruthy();
  });
});
