import { RecipeFilterTitlePipe } from './recipe-filter-title.pipe';

describe('RecipeFilterTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeFilterTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
