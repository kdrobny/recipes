import { RecipeCategoryListPipe } from './recipe-category-list.pipe';

describe('RecipeCategoryListPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeCategoryListPipe();
    expect(pipe).toBeTruthy();
  });
});
